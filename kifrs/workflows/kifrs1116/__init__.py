"""K-IFRS 1116 (리스) 결정 엔진 — Phase 4 `data/scenarios/1116_lease/WORKFLOW.md` 이식 (AE1).

WA1(1109) 패턴을 그대로 복제한다(docs/plans/2026-07-04-ae1-1116-lease-engine.md): 구조화 거래
입력 → 식별(§1) → 분류/면제(§2) → 최초측정(§3) → 후속측정(§4) → 재평가·변경(§5) → 검토메모(§6),
런타임 citation grounding(RGA1) 포함.

AE1 scope: 리스이용자(단순·4구성요소·면제·매수선택권·기간재평가·기간연장 변경)와 리스제공자
(금융리스 후속, 금융↔운용/금융→금융 변경)를 결정론적으로 자동화한다. **동시 확장+축소 변경**
(scenario_09)은 [1116-46(a)] 축소분 PL 인식과 [1116-45]/[1116-46(b)] 재측정을 2차원으로 분해해야
하고, 단일-차원 변경과 달리 별도 결정 로직이 필요해 이번 범위 밖 — `run_lease()`가 이런 입력을
만나면 `NeedsHumanReview`를 던진다(회귀 하네스가 완료율 분모에는 포함하되 "사람 개입 필요"로
정직하게 집계). 판단값(리스제공자 분류·변경 유형·행사 확실성·할인율·현가계수)은 WA1의
SPPI/사업모형 판단과 동일하게 **입력**으로 받는다 — 판단 자체의 자동화는 범위 밖이다.
"""
