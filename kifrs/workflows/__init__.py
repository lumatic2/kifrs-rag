"""Executable decision-engine workflows (Workflow Automation horizon, 2026-07-03).

Each submodule ports one domain's `data/scenarios/<domain>/WORKFLOW.md` decision
tree into callable code, so the same input always produces the same classification,
entries, and review memo — instead of a human (or Claude) re-walking a document.
"""
